# AI-Stock 一阶段启动说明

## 1. 配置步骤

### 步骤1: 复制配置文件
```bash
# 将 appsettings.user.json 中的配置填入 appsettings.json
```

### 步骤2: 修改 appsettings.json
编辑 `AIStock.Web/appsettings.json`，填写以下配置：

```json
{
  "ConnectionStrings": {
    "MySQL": "Server=localhost;Port=3306;Database=aistock;User=你的用户名;Password=你的密码;",
    "Redis": "localhost:6379,password=你的Redis密码"
  },
  "DataProviders": {
    "Sanhu": {
      "Token": "你的散户量化Token"
    }
  }
}
```

## 2. 数据库初始化

### 方式1: 使用SQL脚本
```bash
mysql -u root -p < scripts/database/init.sql
```

### 方式2: 自动创建
项目启动时会自动创建数据库表（EnsureCreated）

## 3. 启动项目

### 方式1: 命令行启动
```bash
cd AIStock.Web
dotnet run
```

### 方式2: 使用Docker
```bash
docker-compose up -d
```

## 4. 验证服务

访问以下地址验证服务是否正常：
- 健康检查: http://localhost:5000/api/health
- 股票列表: http://localhost:5000/api/stock/list
- Swagger: http://localhost:5000/swagger

## 5. API接口说明

| 接口 | 方法 | 说明 |
|------|------|------|
| /api/health | GET | 健康检查 |
| /api/stock/list | GET | 获取股票列表 |
| /api/stock/{code}/quote | GET | 获取实时行情 |
| /api/stock/{code}/kline | GET | 获取K线数据 |
| /api/stock/{code}/intraday | GET | 获取分时数据 |
| /api/stock/providers/status | GET | 数据源状态 |

## 6. 常见问题

### MySQL连接失败
- 确认MySQL服务已启动
- 检查用户名密码是否正确
- 确认数据库aistock已创建

### Redis连接失败
- 确认Redis服务已启动
- 检查密码是否正确

### 散户量化API失败
- 检查Token是否有效
- 确认网络可访问
