# 选股策略框架 + 因子库 + 数据分层 — 开发计划与进度

> 记录"可配置策略框架 / 因子库扩展 / 选股数据分层（盘中快照 vs 回测重建）/ 尾盘买入策略"的设计、进度与待办，供后续会话接续。

## 总体目标
1. **可配置策略框架**：前端组合"过滤口径 + 因子口径 + 权重"创建新策略，无需改代码部署。
2. **因子库扩展**：在现有 8 因子上增加经回测验证有效的新因子。
3. **数据分层明确**：盘中实时快照（`daily_market_snapshot`）服务盘中/尾盘选股；收盘后选股与回测走 `kline+capital_flow` 重建。
4. **尾盘买入策略**：盘中/尾盘用实时快照选股、尾盘买入；回测用 `entry=SignalClose` 对齐。

---

## ✅ 已完成（commit 616cb15 + 后续修正提交）

### 阶段 1：可配置策略框架
- 新增 `strategy_definition` 表（迁移已落 RDS）+ `StrategyDefinition` 模型 + `ConfigurableSelectionStrategy`
- `ISelectionStrategyProvider`/`SelectionStrategyProvider`：合并"内置 + DB 自建"，同 key 内置优先
- `BuiltinStrategyDefinitions`（内置三策略定义表达，模板/验收用）
- 改造 `StockSelectionService`/`ReplayBacktestService`/MCP/Controller 注入
- **验收**：127 单测过；等价性测试证明可精确复刻内置 trend/lowdip/theme

### 阶段 2-因子①：Volatility（波动/风险）— 有效，已定版 lowdip v2.1
- `SequenceFeatures.AvgAmplitude5` + `SelectionScorers.Volatility`（低波动=高分），接入 8 因子加权（默认权重 0）
- 已存为 **lowdip v2.1（Weights.Volatility=0.10）并激活**（selection_config Id=3）
- **回测验证（同一次运行内对比 v2.0 vs v2.1）**：
  - NextOpen(次日开盘) 30天：回撤 115→66、盈亏比 1.02→1.29、均收益 -0.40%→+0.65%、胜率 46.7→48.6%
  - **SignalClose(尾盘买入)** 30天：回撤 145→84、盈亏比 1.07→1.29、均收益 -0.82%→+0.08%、胜率 41.7→44.3%
  - SignalClose 60天：回撤 181→136、最差单笔 -25→-17（胜率/均收益基本持平）
  - 核心价值：**压回撤 + 收敛尾部风险**，尾盘与次日开盘两口径下方向一致
- ⚠️ v2.1 实盘生效需 Web/Worker 运行含 Volatility 因子的新代码

### 阶段 2-因子②：RelativeStrength（相对大盘强度）— 已证无效，不纳入
- 代码保留为可选因子（默认权重 0、不启用、无害），任何正权重都让指标全面变差

### 回测引擎：尾盘买入支持
- MCP `replay_backtest` 工具新增 `entry` 参数（`NextOpen` | `SignalClose`），对齐 `BacktestEngine` 已有的信号日收盘买入。**尾盘买入策略回测 = `entry=SignalClose`**（信号日收盘价 ≈ 尾盘买入价）

---

## 📌 现状澄清：daily_market_snapshot 已是"盘中实时快照层"（无需改造）
> 之前一度误判它"只是收盘后东财采集、需要改造回盘中实时"——错误，纠正如下。

`MarketSnapshotSyncService` 有两个方法，由 `MarketSnapshotSyncJob` 按 `EnableIntraday`（默认 true）选择：
- **盘中（默认）**：`SyncIntradayAsync` —— 每 `IntradayIntervalMinutes`(默认20) 分钟，**腾讯批量实时报价** + 库内历史日K拼当日 bar + `IFeatureCalculator` 算动态指标 + 东财批量主力净流入 → upsert `daily_market_snapshot` 当日行。**这就是盘中/尾盘实时选股的数据源。**
- **收盘后/未启用盘中**：`SyncAsync` —— 东财逐只采集。

**数据分层全貌**：
| 层 | 数据源 | 用途 |
|---|---|---|
| 盘中实时 | `daily_market_snapshot`（腾讯实时报价+东财主力流入+IFeatureCalculator） | 盘中/尾盘实时选股（实盘 `LoadAsync` 读它）|
| 收盘/历史 | `kline_data`+`daily_capital_flow`（`SnapshotRebuilder`+IFeatureCalculator） | 回测；可选作收盘后选股 |

**残留差异（轻）**：盘中行情源是腾讯、回测重建用东财日K，源不同有细微口径差异；但技术指标用**同一个 `IFeatureCalculator`**，差异远小于早先判断。

---

## ⬜ 待办

### 阶段 2-因子③：CapitalStructure（资金结构）
- 四档资金（超大/大/中/小单）仅在 `daily_capital_flow`；盘中 snapshot 只有 main 合计。
- 先做**轻量回测验证**（`SnapshotRebuilder` 从 capital_flow 填四档 + 因子：超大单主导/散户背离），有效再考虑实盘。
- 前两次资金类尝试（接飞刀/派发、RelStr）均无效/冗余，边际价值存疑，务必先验证。

### 阶段 4：前端策略管理 + CRUD API
- `strategy-def` CRUD API（GET/POST/PUT/DELETE）+ key 唯一性/内置不可删校验
- 前端"策略管理"页（或扩展 `web/src/pages/SelectionConfig.tsx`）：因子口径下拉 + 过滤开关 + 权重编辑 + "激活前先回测"引导
- 后端已支持：手动插一行 `strategy_definition` 即可端到端跑通

### 尾盘买入策略（主线）
- 用 `entry=SignalClose` 重新评估所有调参（v2.1 已验证；后续新因子/新参数都用尾盘口径）
- 可选：精细化尾盘入场建模（当前用收盘价近似 14:55 尾盘价，差异小）

---

## 重要笔记（踩过的坑）
1. **回测数据在漂移**：Worker 持续采集会改 `kline_data`/`capital_flow`，同参数回测绝对值随时间变；因子对比必须**同一次运行内**做。
2. **盘中 vs 回测口径**：盘中 snapshot 用腾讯实时、回测重建用东财日K，源不同有细微差异（技术指标同 IFeatureCalculator）。
3. **加因子 ≠ 有效**：引擎算法增强（接飞刀/回踩协同/派发惩罚）和 RelativeStrength 均经回测证伪并回滚/不启用。每个新因子必须回测验证。
4. **买入时点**：MCP `replay_backtest` 默认 `NextOpen`（次日开盘）；尾盘策略须显式传 `entry=SignalClose`。早期 v2.0/v2.1 调参用的是 NextOpen，已用 SignalClose 复核 v2.1 仍成立。
5. **MCP 调用**：本会话靠 `scripts/mcp_call.ps1` stdio 驱动 `AIStock.Mcp.exe`（需设 `$env:MCP_MYSQL`、`$env:MCP_EXE`）。**脚本内勿写中文**（PowerShell 5.1 按 GBK 读无 BOM UTF-8 .ps1 会乱码破坏语法）。
6. **重建 MCP 的锁**：Claude Desktop 长驻 Release 版锁 `bin/Release`；改完用 **Debug** 构建并 `$env:MCP_EXE` 指向 Debug exe 测试，Desktop 用新版需重启。

## 工具脚本：scripts/mcp_call.ps1
```powershell
$env:MCP_MYSQL = "Server=...;Database=aistock;User=...;Password=...;CharSet=utf8mb4;"  # 必填，勿提交明文
$env:MCP_EXE   = "...\AIStock.Mcp\bin\Debug\net9.0\AIStock.Mcp.exe"                      # 可选，默认 Release
$req = '[{"name":"replay_backtest","args":{"strategy":"lowdip","criteriaJson":"","from":"2026-05-06","to":"2026-06-05","holdDays":5,"entry":"SignalClose"}}]'
& scripts\mcp_call.ps1 -RequestsJson $req
```
